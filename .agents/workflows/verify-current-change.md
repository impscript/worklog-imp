# Verify current change

Verify the current Worklog Newgen change without broadening its scope.

1. Load and follow `.agents/skills/worklog-verify-change/SKILL.md`.
2. Derive the verification scope from the staged and unstaged diff.
3. Run the applicable build, focused lint, and safe local checks.
4. Report each check as passed, failed, or not run with a reason.
