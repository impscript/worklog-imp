# Review current change

Perform a read-only review of the current Worklog Newgen change.

1. Load and follow `.agents/skills/worklog-code-review/SKILL.md`.
2. Review both staged and unstaged changes unless the user names a narrower scope.
3. Do not modify files, stage changes, commit, push, deploy, or mutate data.
4. Return findings first, followed by checks run and residual risk.
