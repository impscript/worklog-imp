# Worklog Newgen project rules

Use `@../../AGENTS.md` as the authoritative repository guidance for every task in this workspace.

Before editing, inspect the working tree and relevant code path. Keep changes focused, protect secrets and remote data, run the applicable verification, and leave a handoff checkpoint when work will continue in another agent.
